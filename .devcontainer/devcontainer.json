{
    "name": "GreenMoStalker",
    "build": {
        "dockerfile": "Dockerfile"
    },
    // Running in priviledged mode to allow access to docker.
    "runArgs": ["--volume=/var/lib/docker", "--privileged"]

    // Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
    // "remoteUser": "root"
}
