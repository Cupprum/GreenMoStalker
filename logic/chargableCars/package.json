{
    "name": "chargablecars",
    "dependencies": {
        "@aws-lambda-powertools/parameters": "^1.14.0",
        "@aws-sdk/client-ssm": "^3.438.0",
        "axios": "^1.6.0"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.124",
        "@types/jest": "^29.5.7",
        "@types/node": "^20.8.5",
        "esbuild": "^0.19.5",
        "jest": "^29.7.0",
        "ts-jest": "^29.1.1",
        "typescript": "^5.2.2"
    },
    "scripts": {
        "test": "jest",
        "build": "npx esbuild --bundle --platform=node --outfile=out.js ./lib",
        "run": "node out.js"
    },
    "jest": {
        "preset": "ts-jest"
    }
}
